<h1>Places</h1>
<a href="/places/new" class="btn btn-primary mt-2">New Place</a>
<ul class="list-unstyled mt-3">
  <% @places.each do |place| %>
    <li class="mb-2">
      <a href="<%= place_path(place) %>" class="text-decoration-underline"><%= place.name %></a>
    </li>
  <% end %>
</ul>